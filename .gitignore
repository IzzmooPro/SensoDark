# Build artifacts
dist/*.zip

# Local backups (kept out of the shipped extension; history has the original)
backup/

# Local tooling / editor state
.claude/settings.local.json

# OS noise
Thumbs.db
Desktop.ini
.DS_Store
